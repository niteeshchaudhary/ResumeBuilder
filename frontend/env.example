# Interview Scheduling System Environment Variables
# Copy this file to .env and update the values as needed

# Backend API URL (required for interview booking)
VITE_API_URL=http://localhost:8000/reserish

# Note: In Vite, environment variables must be prefixed with VITE_
# Only variables with VITE_ prefix will be available in the browser
